// @roland/core (file:../) gives us the `roland` binary at node_modules/.bin/roland.
// The run route spawns it as a subprocess — no direct TS import is needed.
// Add typed imports here if you expose types from the Roland package in future.
