.next/
dist/
node_modules/
*.db
*.db-journal
.env
.env.local
.DS_Store

# Cloned project repos — never commit user workspaces
projects/

# Roland run state — runtime files, may contain goal/output text
.roland/

# @roland-core is populated by setup:core / Docker — never commit compiled output
@roland-core/dist/
@roland-core/agents/
@roland-core/recipes/
