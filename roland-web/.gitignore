.next/
dist/
node_modules/
*.db
*.db-journal
.env
.env.local
.DS_Store

# Cloned project repos — never commit user workspaces
projects/

# Roland run state — runtime files, may contain goal/output text
.roland/

# @roland-core/dist, agents, recipes are committed so Railway needs no root-level build.
# Rebuild locally with: npm run build:core
