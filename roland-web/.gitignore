.next/
/dist/
!@roland-core/dist/
!@roland-core/agents/
!@roland-core/recipes/
node_modules/
*.db
*.db-journal
.env
.env.local
logs/
data/
.DS_Store

# Cloned project repos — never commit user workspaces
projects/

# Roland run state — runtime files, may contain goal/output text
.roland/

# @roland-core/dist, agents, recipes are committed so install.sh needs no root-level build.
# Rebuild locally with: npm run build:core
