## P0 Final Bugfix — roland-web Recipe Mirror
# @roland-core/dist, agents, recipes are build-time mirrors — never commit.
# Rebuild with: npm run build:core (or install.sh / update.sh)
# Audit #3 (v1.4.0): only index.js + package.json are tracked under @roland-core/.

.next/
/dist/
@roland-core/dist/
@roland-core/agents/
@roland-core/recipes/
node_modules/
*.db
*.db-journal
.env
.env.local
logs/
data/
.DS_Store

# Cloned project repos — never commit user workspaces
projects/

# Roland run state — runtime files, may contain goal/output text
.roland/
