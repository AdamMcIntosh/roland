{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "if [ ! -d @roland-core/dist ]; then npm ci --prefix .. && npm run build --prefix .. && node scripts/setup-core.mjs && npm install; fi && npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "numReplicas": 1,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
